# Tester-server runtime + test-tooling dependencies.
# These packages are installed in the Dockerfile.tester runtime stage.
pytest==8.3.4
pytest-asyncio==1.3.0
certifi==2026.2.25
fastapi==0.135.1
uvicorn==0.42.0
pydantic==2.12.5
requests==2.33.0
mcp==1.26.0
mcp-watchdog==0.1.9
